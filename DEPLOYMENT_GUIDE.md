# Руководство по деплою сайта ПроЙога

## 🚀 Шаг 1: Подготовка к загрузке на сервер

### Проверьте готовность проекта
```bash
# Убедитесь что все работает локально
php artisan check:email-system

# Соберите frontend assets
npm run build

# Проверьте что нет ошибок
php artisan config:clear
php artisan cache:clear
```

### Создайте архив для загрузки
1. **Исключите ненужные файлы**:
   - `node_modules/` (будет установлен на сервере)
   - `.env` (создадите новый на сервере)
   - `storage/logs/*` (логи локальной разработки)
   - `.git/` (если не нужен на сервере)

2. **Обязательно включите**:
   - Все файлы `app/`
   - `public/`
   - `resources/`
   - `database/`
   - `routes/`
   - `config/`
   - `composer.json`, `package.json`
   - `EMAIL_SETUP_GUIDE.md`

## 🌐 Шаг 2: Загрузка на хостинг

### Через FileZilla/cPanel File Manager:
1. **Подключитесь к хостингу**
2. **Загрузите файлы в корневую папку** (обычно `public_html/` или `www/`)
3. **Установите права доступа**:
   - `storage/` и `bootstrap/cache/` - 755 или 775
   - Файлы `.env` - 644

### Через Git (если хостинг поддерживает):
```bash
git clone https://github.com/StZDerr/proyoga.git
cd proyoga
```

## ⚙️ Шаг 3: Настройка на сервере

### 1. Создайте .env файл
Скопируйте содержимое из `.env.example` и настройте:

```env
APP_NAME="ПроЙога"
APP_ENV=production
APP_KEY=base64:СГЕНЕРИРУЙТЕ_НОВЫЙ_КЛЮЧ
APP_DEBUG=false
APP_URL=https://йога-истоки.рф

# База данных (узнайте у хостинга)
DB_CONNECTION=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=название_базы
DB_USERNAME=пользователь_базы  
DB_PASSWORD=пароль_базы

# Email настройки (см. ниже)
MAIL_MAILER=smtp
MAIL_HOST=smtp.yandex.ru
MAIL_PORT=587
MAIL_USERNAME="info@xn--h1aafpog8g.xn--p1ai"
MAIL_PASSWORD="ПАРОЛЬ_ПРИЛОЖЕНИЯ"
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="info@xn--h1aafpog8g.xn--p1ai"
MAIL_FROM_NAME="ПроЙога"

CONTACT_EMAIL="info@xn--h1aafpog8g.xn--p1ai"
```

### 2. Установите зависимости
```bash
# Composer (обязательно)
composer install --no-dev --optimize-autoloader

# Node.js (если нужно пересобрать assets)
npm ci
npm run build
```

### 3. Настройте Laravel
```bash
# Сгенерируйте ключ приложения
php artisan key:generate

# Создайте символическую ссылку для storage
php artisan storage:link

# Настройте кеш для продакшена
php artisan config:cache
php artisan route:cache
php artisan view:cache

# Выполните миграции (если используете базу данных)
php artisan migrate --force
```

## 📧 Шаг 4: Настройка email отправки

### Вариант 1: Yandex Mail (Рекомендуется)

1. **Войдите в почту info@йога-истоки.рф**
2. **Создайте пароль приложения**:
   - Откройте https://passport.yandex.ru/
   - Перейдите в "Безопасность"
   - Найдите "Пароли приложений"
   - Нажмите "Создать пароль приложения"
   - Выберите "Почтовый клиент"
   - Скопируйте сгенерированный пароль

3. **Обновите .env на сервере**:
```env
MAIL_MAILER=smtp
MAIL_HOST=smtp.yandex.ru
MAIL_PORT=587
MAIL_USERNAME="info@xn--h1aafpog8g.xn--p1ai"
MAIL_PASSWORD="скопированный_пароль_приложения"
MAIL_ENCRYPTION=tls
```

### Вариант 2: Хостинг с sendmail
Многие хостинги поддерживают sendmail:
```env
MAIL_MAILER=sendmail
```

### Вариант 3: SMTP хостинга
Узнайте у хостинга SMTP настройки и используйте их.

## 🧪 Шаг 5: Тестирование

### Проверьте email систему:
```bash
php artisan check:email-system
```

### Если команда недоступна, проверьте вручную:
1. Заполните форму на сайте
2. Проверьте логи: `storage/logs/laravel.log`
3. Проверьте почту info@йога-истоки.рф

## 🔧 Шаг 6: Настройка веб-сервера

### Apache (.htaccess уже настроен)
Убедитесь что `mod_rewrite` включен.

### Nginx
Добавьте в конфиг:
```nginx
location / {
    try_files $uri $uri/ /index.php?$query_string;
}

location ~ \.php$ {
    fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
    fastcgi_index index.php;
    fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
    include fastcgi_params;
}
```

## 🛠️ Возможные проблемы и решения

### Email не отправляется:
1. **Проверьте логи**: `storage/logs/laravel.log`
2. **Проверьте права доступа** к папке `storage/`
3. **Убедитесь в правильности SMTP настроек**
4. **Проверьте, не блокирует ли хостинг SMTP**

### Ошибки прав доступа:
```bash
# Установите правильные права
chmod -R 755 storage bootstrap/cache
chown -R www-data:www-data storage bootstrap/cache
```

### Ошибки базы данных:
1. Проверьте настройки подключения в `.env`
2. Убедитесь что база данных создана
3. Выполните миграции: `php artisan migrate`

### Не работают маршруты:
1. Очистите кеш: `php artisan route:clear`
2. Проверьте настройки веб-сервера
3. Убедитесь что файл `public/.htaccess` загружен

## 📞 Полезные команды для диагностики

```bash
# Проверка конфигурации
php artisan config:show mail

# Проверка маршрутов
php artisan route:list | grep contact

# Очистка всех кешей
php artisan optimize:clear

# Проверка email системы
php artisan check:email-system

# Просмотр логов
tail -f storage/logs/laravel.log
```

## ✅ Чек-лист деплоя

- [ ] Файлы загружены на сервер
- [ ] .env файл создан и настроен
- [ ] composer install выполнен
- [ ] php artisan key:generate выполнен
- [ ] Права доступа настроены (755 для storage/)
- [ ] База данных подключена
- [ ] php artisan migrate выполнен (если нужно)
- [ ] Email настройки проверены
- [ ] Тестовое письмо отправлено
- [ ] Форма обратной связи работает
- [ ] Сайт открывается без ошибок

## 🎯 Контакты для поддержки

После деплоя, если возникнут проблемы:
1. Проверьте логи в `storage/logs/laravel.log`
2. Убедитесь что все команды из чек-листа выполнены
3. Проверьте настройки хостинга (PHP версия, расширения)

**Удачного деплоя! 🚀**